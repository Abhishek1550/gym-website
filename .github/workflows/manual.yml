name: manual-trigger

on: 
    workflow_dispatch:


jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            -   name: Build the code
                run: echo "Building the code..."