name: manual-trigger

on: 
    workflow_dispatch:


jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            -   name: Build the code
                timeout-minutes: 1
                run: sleep 180